// pages/home.tsx

import ColorGrid from '@/components/grid/ColorGrid';

const Home = () => {
  return (
    <section className="w-full h-full">
      <ColorGrid />
    </section>
  );
};

export default Home;